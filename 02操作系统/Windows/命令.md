# 1. cmd下的命令

dxdiag
	查看电脑配置



**CRLF 不是单独的 `\r`，而是 `\r\n`**。

- **CR**（Carriage Return，回车）：`\r`（ASCII 13）
    
- **LF**（Line Feed，换行）：`\n`（ASCII 10）
    
- **CRLF**：`\r\n`（两个字符的组合）
    

Windows 系统使用 `\r\n` 作为换行符，而 Linux/macOS 使用 `\n`。