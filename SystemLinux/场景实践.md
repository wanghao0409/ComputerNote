对future 用户python库授权：
```bash
sudo chmod -R 755 /usr/local/lib64/python3.11/site-packages/

sudo chmod -R 755 /usr/local/lib/python3.11/site-packages/pytz
```
# Future 


```bash

启动服务：
sudo systemctl start ccxt_trading
sudo systemctl start ccxt_trading_price_view

查看服务状态：
sudo systemctl status ccxt_trading

sudo systemctl show ccxt_trading

查看日志：
sudo journalctl -u ccxt_trading -f
sudo journalctl -u ccxt_trading_price_view -f
停止服务
sudo systemctl stop ccxt_trading

监控服务日志  # 用这个
watch -n 1 "sudo journalctl -u ccxt_trading --since '1 minute ago' -n 14 --no-pager"
		
		
Cat /var/log/syslog | tail -n 10		
```
Trae 新对话



